id: door
abstract: true
extends: single_tile_node

states:
  open:
    default: false
    values:
      true:
        parts:
          main:
            alpha: 0.1
            scale: 0.3
  rotting:
    default: false
    values:
      true:
        parts:
          main:
            filters: [grayscale]
        animations: [rotting]

animations:
  rotting:
    preset: rotting
    parts: main
