import { WORLD_BOUNDS } from "@bundu/shared/tiles";

/** Client sky/viewport extent — matches server playable bounds. */
export const WORLD_SIZE = WORLD_BOUNDS;

export const ITEM_BUTTON_SIZE = 55;
